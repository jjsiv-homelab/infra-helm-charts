# Name of the tenant.
name:
# Metadata to apply to the tenant-{{ .Values.name }} namespace that will be created.
namespaceMetadata:
  labels: {}
  annotations: {}

# More fields to come eventually, repos, auth, etc.
